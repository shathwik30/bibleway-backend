# Core
Django>=5.1,<6.0
djangorestframework>=3.15,<4.0
djangorestframework-simplejwt>=5.3,<6.0
django-cors-headers>=4.3,<5.0
django-filter>=24.1,<25.0

# Database
dj-database-url>=2.1,<3.0
psycopg[binary]>=3.1,<4.0

# Async / WebSocket
channels>=4.0,<5.0
channels-redis>=4.2,<5.0
daphne>=4.1,<5.0

# Task Queue
celery>=5.4,<6.0
redis>=5.0,<6.0

# Push Notifications (via Expo Push API — no server-side SDK needed)
# The backend POSTs to exp.host; see apps/notifications/tasks.py

# Email
resend>=2.0,<3.0

# Firebase Admin (Google Auth verification + future push)
firebase-admin>=6.0,<7.0

# Google Play Store Validation
google-api-python-client>=2.100,<3.0

# Environment
python-decouple>=3.8,<4.0

# Object Storage (S3-compatible)
boto3>=1.34,<2.0
django-storages>=1.14,<2.0

# HTTP Client
requests>=2.31,<3.0

# Utilities
Pillow>=10.2,<11.0
gunicorn>=22.0,<23.0

# Production
whitenoise>=6.6,<7.0
